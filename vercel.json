{
  "functions": {
    "api/*.go": {
      "runtime": "go"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/main.go"
    }
  ],
  "buildCommand": "go build -o api/main api/main.go",
  "outputDirectory": ".",
  "installCommand": "go mod download"
} 